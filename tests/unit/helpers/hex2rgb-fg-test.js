import { hex2rgbFg } from '../../../helpers/hex2rgb-fg';
import { module, test } from 'qunit';

module('Unit | Helper | hex2rgb fg');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hex2rgbFg(42);
  assert.ok(result);
});
